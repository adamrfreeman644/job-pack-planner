services:
  job-pack-planner:
    build: .
    container_name: job-pack-planner
    ports:
      - "1976:1976"
    volumes:
      - ./data:/data
      - ${JOB_PACK_SHARE_PATH:-./shared}:/imports
    environment:
      TZ: Europe/London
      JOB_PACK_SHARE: /imports
    restart: unless-stopped
